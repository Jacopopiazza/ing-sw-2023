package it.polimi.ingsw.Messages;

/**
 * The GameServerMessageTicket class represents an empty message to let a GameServerMessage information go through the network.
 * It extends the {@code Message} abstract class and is serializable.
 */
public class GameServerMessageTicket extends Message {

    /**
     * Default constructor for {@code GameServerMessageTicket}.
     */
    public GameServerMessageTicket() {
    }
}
